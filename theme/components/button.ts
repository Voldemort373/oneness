import { ComponentStyleConfig } from "@chakra-ui/react";

export const buttonStyle: ComponentStyleConfig = {
    baseStyle: {
        borderRadius: "50rem",
        fontWeight: "600",
        fontFamily: "body"
    }
}